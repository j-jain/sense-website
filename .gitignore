.DS_Store
.vercel
node_modules/
*.log
dist/

# Local dev / editor config
.claude/

# Dev-only prototype & concept pages (not part of the shipped site)
# NOTE: src/scenes/proto-laptop/ is now spliced into the shipped flow (referenced by
# index.html / main.js / main.css), so it is tracked — do NOT re-ignore it or the deploy breaks.
laptop-proto.html
s5c-laptop-proto.html
src/scenes/proto-s5c/
public/interstitials.html
